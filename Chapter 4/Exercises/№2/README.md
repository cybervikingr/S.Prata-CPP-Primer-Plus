### 2. Перепишите листинг 4.4, применив класс С++ ``string`` вместо массива ``char``.

------------------------------------------------

### 2. Rewrite Listing 4.4, using the C++ ``string`` class instead of ``char`` arrays.